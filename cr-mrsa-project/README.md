# Mini RSA Project (cr-mrsa-project)

64비트 길이의 Mini RSA 알고리즘을 C로 구현한 암호학 과제 프로젝트입니다.  
실제 서비스에서 사용하는 2048비트 RSA에 비해 단순하지만,  
**키 생성 → 암호화 → 복호화**의 전체 흐름을 모두 포함한 교육용 RSA 구현입니다.

<br>

## 🔑 프로젝트 목표

- 64비트 RSA 모듈러스 \(2^63 ≤ n < 2^64\) 범위를 만족하는 Mini RSA 구현  
- Carmichael 함수 λ(n)을 기반으로 한 공개키·개인키 생성  
- Miller–Rabin 소수 판별로 32비트 소수 p, q 생성  
- `m^k mod n` 형태의 모듈러 거듭제곱 기반 암·복호화 구현  

사용자가 호출하는 공개 인터페이스는 다음과 같습니다:

```c
void mRSA_generate_key(uint64_t *e, uint64_t *d, uint64_t *n);
int  mRSA_cipher(uint64_t *m, uint64_t k, uint64_t n);
<br>
🧩 핵심 기능
기능	설명
키 생성	32비트 소수 p, q를 Miller–Rabin으로 생성하고, 64비트 모듈러스 n = p·q 구성
Carmichael 함수	λ(n) = lcm(p − 1, q − 1)을 이용해 공개키 e, 개인키 d 계산
모듈러 연산	mod_add, mod_mul, mod_pow, gcd, umul_inv 등 RSA 필수 연산 구현
소수 판별	64비트 범위에서 결정적 Miller–Rabin(기저 12개) 사용
암·복호화	mRSA_cipher()로 m^k mod n 수행, 범위 검사·에러 처리 포함
랜덤 테스트	제공된 테스트 코드 기준 Random testing PASS

<br>
🛠 사용 기술
언어: C (C99)

난수: arc4random, arc4random_uniform 기반 안전한 난수 생성

수론 알고리즘:

유클리드 호제법

확장 유클리드 알고리즘

모듈러 거듭제곱

모듈러 역원

환경: Linux / macOS / WSL 등 POSIX 계열 환경에서 컴파일 가능

<br>
📁 폴더 구조
plaintext
코드 복사
cr-mrsa-project/
├── src/
│   ├── mRSA.c               # Mini RSA 핵심 구현
│   └── test.c               # 교수님 제공 테스트 코드
├── include/
│   └── mRSA.h               # 함수 프로토타입 및 상수 정의
├── result/
│   └── mRSA.txt             # 실행 결과 로그
├── Makefile                 # 빌드 스크립트
└── README.md                # 프로젝트 설명 파일
<br>
⚙️ 빌드 & 실행
bash
코드 복사
# 빌드
make

# 실행
./mRSA
실행이 성공적으로 완료되면 result/mRSA.txt 파일에
생성된 키(e, d, n)와 여러 메시지 m에 대한 암·복호화 결과가 저장됩니다.

<br> ```